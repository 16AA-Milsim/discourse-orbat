en:
  orbat:
    empty_placeholder: "Vacant"
    errors:
      invalid_json: "Configuration JSON is invalid: %{message}"
      missing_group: "Group \"%{group}\" is not recognised."
      hidden_group: "Group \"%{group}\" is hidden from the ORBAT output."
  orbat_admin:
    errors:
      missing_default: "Default ORBAT configuration is unavailable."
      invalid_join_date: "Join date must be a valid date (YYYY-MM-DD)."
  site_settings:
    orbat_enabled: "Enable the ORBAT plugin"
    orbat_admin_only: "Restrict the ORBAT page to admins"
    orbat_cache_ttl: "ORBAT cache TTL (seconds)"
    orbat_hide_hidden_groups: "Hide staff-only groups from ORBAT"
    orbat_exclusive_groups: "ORBAT exclusive groups"
